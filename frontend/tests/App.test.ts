import { render, screen, cleanup } from "@testing-library/react";
import HomePage from "../src/components/pages/Unlogged/HomePage";
describe("render home page", () => {
  it("render application ", () => {
    //  render(<App/>);
  });
});
